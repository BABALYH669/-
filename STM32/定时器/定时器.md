---
notion-id: 3465d378-8ef5-801a-8930-ca66d4ae6e6c
---
[[基本-通用-高级定时器总结]]

时钟源和时钟树

![[TapTapLoot_kNkJrUJw9N.png]]

![[TapTapLoot_Ojt8SqL6uy.png]]

![[TapTapLoot_J4tZvFd9wn.png]]

# 基本定时器

基本定时器:TIM6/TIM7

主要特性:16位递增计数器(2^16 = 65536 = 0~65535) 
                  16位预分频器(分频系数1~65536)
                  可用于触发DAC
                  在更新事件(计数器溢出)时,可产生中断/DMA请求

![[TapTapLoot_MTdc8lMlMv.png]]

影子寄存器是实际起作用的寄存器,不可直接访问  U(update)更新 
事件默认是产生的,DMA和中断默认不产生的
溢出条件:CNT == ARR(影子)  
UG位可产生软件更新事件
ARPE位决定ARR是否具有缓冲,
有缓冲:ARR会等有事件产生才把值写入到影子寄存器,需要更改ARR的值,有缓冲会减小一定的误差
无缓冲:ARR会直接把值写入到影子寄存器,不需要改变ARR的值可以无缓冲

定时器2~7挂载到APB1总线上,APB1二分频了(72HMz/2 = 36MHz),根据预分频=1不变,否则乘2这个规则来说,定时器2~7 的工作频率就是 36MHz*2 = 72MHz

STM32定时器计数模式及溢出条件:

![[TapTapLoot_zm5Puxlri4.png]]

寄存器

![[TapTapLoot_ItORt6S1fS.png]]

![[TapTapLoot_0cubReB67N.png]]

![[TapTapLoot_nUH2jAZMCK.png]]

![[TapTapLoot_RK2P5AYacz.png]]

![[TapTapLoot_bmx74C00CX.png]]

![[TapTapLoot_bfUZIUr44N.png]]

![[TapTapLoot_r9czT347aJ.png]]

![[TapTapLoot_5ZtXMdfCRZ.png]]

![[TapTapLoot_HsVV8Yf0ph.png]]

```c
typedef struct 
{     
TIM_TypeDef *Instance;         /* 外设寄存器基地址 */     
TIM_Base_InitTypeDef Init;     /* 定时器初始化结构体*/    
}TIM_HandleTypeDef;
```

```c
typedef struct 
{     
uint32_t Prescaler;         /* 预分频系数 */     
uint32_t CounterMode;       /* 计数模式 */     
uint32_t Period;            /* 自动重载值 ARR */     
uint32_t ClockDivision;     /* 时钟分频因子 */     
uint32_t RepetitionCounter; /* 重复计数器寄存器的值 高级计数器才有这个功能*/     
uint32_t AutoReloadPreload; /* 自动重载预装载使能 */
} TIM_Base_InitTypeDef;
```

```c
#include "./BSP/LED/led.h"
#include "./BSP/TIMER/btim.h"


TIM_HandleTypeDef g_timx_handle;

/* 定时器中断初始化函数 */
void btim_timx_int_init(uint16_t arr, uint16_t psc)
{
    g_timx_handle.Instance = TIM6;  寄存器基地址
    g_timx_handle.Init.Prescaler = psc; 预分频系数
    g_timx_handle.Init.Period = arr; 自动重载值
    HAL_TIM_Base_Init(&g_timx_handle); 
	
    HAL_TIM_Base_Start_IT(&g_timx_handle);
}

/* 定时器基础MSP初始化函数 */
void HAL_TIM_Base_MspInit(TIM_HandleTypeDef *htim)
{
    if (htim->Instance == TIM6)
    {
        __HAL_RCC_TIM6_CLK_ENABLE();
        HAL_NVIC_SetPriority(TIM6_IRQn, 1, 3);
        HAL_NVIC_EnableIRQ(TIM6_IRQn);
    }
}

/* 定时器6中断服务函数 */
void TIM6_IRQHandler(void)
{
    HAL_TIM_IRQHandler(&g_timx_handle);
}

/* 定时器溢出中断中断回调函数 */
void HAL_TIM_PeriodElapsedCallback(TIM_HandleTypeDef *htim)
{
    if (htim->Instance == TIM6)
    {
        LED0_TOGGLE();
    }
}

```

```c
#include "./BSP/LED/led.h"
#include "./BSP/TIMER/btim.h"

/* 定时器句柄：HAL用它来保存TIM6的配置参数与运行状态 */
TIM_HandleTypeDef g_timx_handle;

/* 定时器中断初始化函数
 * arr: 自动重载值（周期相关）
 * psc: 预分频系数（计数频率相关）
 */
void btim_timx_int_init(uint16_t arr, uint16_t psc)
{
    /* 1) 绑定本句柄到具体外设：TIM6
     *    HAL内部会用 htim->Instance 来判断你操作的是哪个定时器
     */
    g_timx_handle.Instance = TIM6;  /* 选择定时器外设的基地址/编号 */

    /* 2) 预分频：把定时器输入时钟分频后再计数
     *    实际定时器计数频率 = TimerClock / (psc + 1)
     */
    g_timx_handle.Init.Prescaler = psc; /* 预分频系数 */

    /* 3) 自动重载寄存器 ARR：
     *    当计数器 CNT 从0数到 ARR 后溢出，产生更新事件（Update Event）
     *    周期 = (ARR + 1) * (psc + 1) / TimerClock
     */
    g_timx_handle.Init.Period = arr; /* 自动重载值 */

    /* 4) HAL_TIM_Base_Init:
     *    作用：根据 htim->Init 的内容，把定时器基础配置写入硬件寄存器
     *    同时准备中断/更新事件所需的基础设置（例如计数模式、更新中断开关等）
     *
     *    注意：有时不同工程对“中断开关”细节可能还需要在 CubeMX/HAL里保证，
     *    但大多数情况下 HAL 在 Base_Init 后配合 IRQ 就能触发回调。
     */
    HAL_TIM_Base_Init(&g_timx_handle);

    /* 5) 启动带中断的定时器：
     *    HAL_TIM_Base_Start_IT 会开启TIM的更新中断，并开始计数
     *    启动后，TIM6溢出->产生中断->进入 TIM6_IRQHandler -> HAL_TIM_IRQHandler
     */
    HAL_TIM_Base_Start_IT(&g_timx_handle);

    /* --- 这一段总结 ---
     * 这里完成了：选择TIM6 + 配置 PSC/ARR + 初始化 + 开启更新中断 + 启动计数
     */
}

/* 定时器基础MSP初始化函数
 * HAL会在 HAL_TIM_Base_Init(&g_timx_handle) 过程中调用到这里
 * MSP = MCU Support Package（MCU层支持：时钟、GPIO、NVIC等）
 */
void HAL_TIM_Base_MspInit(TIM_HandleTypeDef *htim)
{
    /* 先判断是不是我们要用的 TIM6，避免别的定时器也进入本函数 */
    if (htim->Instance == TIM6)
    {
        /* 1) 使能TIM6外设时钟
         *    没有时钟，寄存器写了也没用，定时器不会工作
         */
        __HAL_RCC_TIM6_CLK_ENABLE();

        /* 2) NVIC优先级设置：
         *    (preempt priority, subpriority) 两级优先级
         *    这里的 1,3 取决于你工程配置的优先级分组
         */
        HAL_NVIC_SetPriority(TIM6_IRQn, 1, 3);

        /* 3) 使能TIM6中断通道 */
        HAL_NVIC_EnableIRQ(TIM6_IRQn);

        /* --- 这一段总结 ---
         * 这段只做“让中断和时钟能用”：
         *    - 开TIM6时钟
         *    - NVIC里启用TIM6_IRQn
         */
    }
}

/* TIM6中断服务函数（ISR）
 * 名字必须严格与中断向量表对应：TIM6_IRQHandler
 * 里面一般只做“交给HAL处理”，让HAL去判断中断来源并调用回调
 */
void TIM6_IRQHandler(void)
{
    /* HAL_TIM_IRQHandler:
     *    读取TIM中断标志位（如更新中断 UIF）
     *    清除对应标志（必要时）
     *    并调用 HAL_TIM_PeriodElapsedCallback() 这种回调
     */
    HAL_TIM_IRQHandler(&g_timx_handle);
}

/* 溢出（更新事件）回调函数：
 * 当TIM6发生更新事件（计数溢出）且中断触发后，会走到这里
 */
void HAL_TIM_PeriodElapsedCallback(TIM_HandleTypeDef *htim)
{
    /* 再次确认是TIM6（防止你未来扩展定时器） */
    if (htim->Instance == TIM6)
    {
        /* 切换LED状态
         * 这里假设LED0_TOGGLE() 是你在 led.h/led.c 中封装好的宏或函数
         */
        LED0_TOGGLE();
    }

    /* --- 这一段总结 ---
     * 回调里只做“最小化工作”：
     * 例如翻转GPIO、设置标志位、触发事件等。
     * 复杂逻辑尽量放到主循环/任务里，避免中断里耗时。
     */
}
```

# 通用定时器

[[通用计时器的相关寄存器及其作用]]

基本定时器:TIM2/TIM3/TIM4/TIM5

主要特性:16位递增,递减,中心对齐计数器(2^16 = 65536 = 0~65535) 
                  16位预分频器(分频系数1~65536)
                  可用于触发DAC,ADC
                  在更新事件(计数器溢出),触发事件,输入捕获,输出比较时,可产生中断/DMA请求
                  4个独立通道,可用于:输入捕获,输出比较,输出PWM,单脉冲模式
                  使用外部信号控制定时器且可实现多个定时器互连的同步电路

四类时钟源: 内部时钟,内部触发输入,外部引脚输入时钟(外部时钟模式二),      通道1和通道2(外部时钟模式一)

![[TapTapLoot_ASpSucLwOf.png]]

![[TapTapLoot_sUx7Su5yqg.png]]

采样频率:N=8必须是8个连续的状态才会跳变,4个低电平4个高电平,会保持低电平

TRGI,ETRF,CK_INT都是上升沿触发,时钟源外部触发极性要是下降沿必须要经过反向才能变成上升沿才会被触发

### PWM配置步骤

![[TapTapLoot_2czLYxBvNR.png]]

![[TapTapLoot_q5XPycDPYw.png]]

---

```c
typedef struct 
{    
uint32_t OCMode; 	  /* 输出比较模式选择 */   
uint32_t Pulse; 	            /* 设置比较值 */   
uint32_t OCPolarity;       /* 设置输出比较极性 */   
uint32_t OCNPolarity;    /* 设置互补输出比较极性 */   
uint32_t OCFastMode;   /* 使能或失能输出比较快速模式 */   
uint32_t OCIdleState;     /* 空闲状态下OC1输出 */   
uint32_t OCNIdleState;  /* 空闲状态下OC1N输出 */ 
} TIM_OC_InitTypeDef;
```

```c
#include "./BSP/TIMER/gtim.h"


TIM_HandleTypeDef g_timx_pwm_chy_handle;

/* 通用定时器PWM输出初始化函数 */
void gtim_timx_pwm_chy_init(uint16_t arr, uint16_t psc)
{
		3.1局部变量
    TIM_OC_InitTypeDef timx_oc_pwm_chy;
    1.配置定时器基础工作参数
	    为什么是TIM3,因为LED0是PB5,PB5的复用定时器就是TIM3_CH2
    g_timx_pwm_chy_handle.Instance = TIM3;
    1.1预分频
    g_timx_pwm_chy_handle.Init.Prescaler = psc;
    1.2重装载值
    g_timx_pwm_chy_handle.Init.Period = arr;
    1.3计数模式
    g_timx_pwm_chy_handle.Init.CounterMode = TIM_COUNTERMODE_UP;
    HAL_TIM_PWM_Init(&g_timx_pwm_chy_handle);
    
    timx_oc_pwm_chy.OCMode = TIM_OCMODE_PWM1;PWM模式1
    timx_oc_pwm_chy.Pulse = arr / 2; 占空比
    timx_oc_pwm_chy.OCPolarity = TIM_OCPOLARITY_LOW; 低电平有效
    3.配置PWM模式/比较值等
    HAL_TIM_PWM_ConfigChannel(&g_timx_pwm_chy_handle, &timx_oc_pwm_chy, TIM_CHANNEL_2);
    4.使能输出并启动计数器
    HAL_TIM_PWM_Start(&g_timx_pwm_chy_handle, TIM_CHANNEL_2);
}
2.定时器PWM输出MSP初始化
/* 定时器输出PWM MSP初始化函数 */
void HAL_TIM_PWM_MspInit(TIM_HandleTypeDef *htim)
{
    if(htim->Instance == TIM3)
    {
        GPIO_InitTypeDef gpio_init_struct;
        使能GPIOB和TIM3时钟
        __HAL_RCC_GPIOB_CLK_ENABLE();
        __HAL_RCC_TIM3_CLK_ENABLE();
				设置GPIO引脚模式
        gpio_init_struct.Pin = GPIO_PIN_5;
        gpio_init_struct.Mode = GPIO_MODE_AF_PP;            /* 推挽复用 */
        gpio_init_struct.Pull = GPIO_PULLUP;                /* 上拉 */
        gpio_init_struct.Speed = GPIO_SPEED_FREQ_HIGH;      /* 高速 */
        HAL_GPIO_Init(GPIOB, &gpio_init_struct);
        
        GPIOB PIN5 的重映射功能
        __HAL_RCC_AFIO_CLK_ENABLE();
        REMAP:重映射
        TIM3_PARTIAL:TIM3的部分重影射
        __HAL_AFIO_REMAP_TIM3_PARTIAL();
    }
}
```

```c
#include "./SYSTEM/sys/sys.h"
#include "./SYSTEM/delay/delay.h"
#include "./SYSTEM/usart/usart.h"
#include "./BSP/LED/led.h"
#include "./BSP/TIMER/gtim.h"


extern TIM_HandleTypeDef g_timx_pwm_chy_handle;     /* 定时器x句柄 */

int main(void)
{
    uint16_t ledrpwmval = 0;
    uint8_t dir = 1;
    
    HAL_Init();                                 /* 初始化HAL库 */
    sys_stm32_clock_init(RCC_PLL_MUL9);         /* 设置时钟,72M */
    delay_init(72);                             /* 初始化延时函数 */
    led_init();                                 /* 初始化LED */
    gtim_timx_pwm_chy_init(500 - 1, 72 - 1);
    
    while(1)
    {
        delay_ms(10);

        if (dir)ledrpwmval++;               /* dir==1 ledrpwmval递增 */
        else ledrpwmval--;                  /* dir==0 ledrpwmval递减 */

        if (ledrpwmval > 300)dir = 0;       /* ledrpwmval到达300后，方向为递减 */
        if (ledrpwmval == 0)dir = 1;        /* ledrpwmval递减到0后，方向改为递增 */

        /* 修改比较值控制占空比 */
        __HAL_TIM_SET_COMPARE(&g_timx_pwm_chy_handle, TIM_CHANNEL_2, ledrpwmval);
    }
}
```

### 通用定时器输入捕获实验配置步骤

![[TapTapLoot_kSVvgaJ98N.png]]

![[TapTapLoot_PYfzaNvGqs.png]]

```c
typedef struct
{     
uint32_t ICPolarity; /* 输入捕获触发方式选择，比如上升、下降沿捕获 */     
uint32_t ICSelection; /* 输入捕获选择，用于设置映射关系 */     
uint32_t ICPrescaler; /* 输入捕获分频系数 */     
uint32_t ICFilter;    /* 输入捕获滤波器设置 */ 
} TIM_IC_InitTypeDef;

```

```c
#include "./BSP/TIMER/gtim.h"


TIM_HandleTypeDef g_timx_cap_chy_handle;     /* 定时器x句柄 */

/* 通用定时器通道y 输入捕获 初始化函数 */
//cap是输入的简写
void gtim_timx_cap_chy_init(uint16_t arr, uint16_t psc)
{
		//设置为0,是因为这个初始化结构体内有很多变量,如果不赋值会匹配内存中的垃圾值
    TIM_IC_InitTypeDef timx_ic_cap_chy = {0};

    g_timx_cap_chy_handle.Instance = TIM5;                              /* 定时器5 */
    g_timx_cap_chy_handle.Init.Prescaler = psc;                         /* 定时器分频 */
    g_timx_cap_chy_handle.Init.CounterMode = TIM_COUNTERMODE_UP;        /* 递增计数模式 */
    g_timx_cap_chy_handle.Init.Period = arr;                            /* 自动重装载值 */
    HAL_TIM_IC_Init(&g_timx_cap_chy_handle);

    timx_ic_cap_chy.ICPolarity = TIM_ICPOLARITY_RISING;                 /* 上升沿捕获 */
    timx_ic_cap_chy.ICSelection = TIM_ICSELECTION_DIRECTTI;             /* 映射到TI1上 */
    timx_ic_cap_chy.ICPrescaler = TIM_ICPSC_DIV1;                       /* 配置输入分频，不分频 */
    timx_ic_cap_chy.ICFilter = 0;                                       /* 配置输入滤波器，不滤波 */
    HAL_TIM_IC_ConfigChannel(&g_timx_cap_chy_handle, &timx_ic_cap_chy, TIM_CHANNEL_1);  /* 配置TIM5通道1 */

    __HAL_TIM_ENABLE_IT(&g_timx_cap_chy_handle, TIM_IT_UPDATE);         /* 使能更新中断 */
    HAL_TIM_IC_Start_IT(&g_timx_cap_chy_handle, TIM_CHANNEL_1);         /* 开始捕获TIM5的通道1 */
}

/* 定时器 输入捕获 MSP初始化函数 */
void HAL_TIM_IC_MspInit(TIM_HandleTypeDef *htim)
{
    if (htim->Instance == TIM5)                             /*输入通道捕获*/
    {
        GPIO_InitTypeDef gpio_init_struct;
        __HAL_RCC_TIM5_CLK_ENABLE();                        /* 使能TIM5时钟 */
        __HAL_RCC_GPIOA_CLK_ENABLE();                       /* 开启捕获IO的时钟 */

        gpio_init_struct.Pin = GPIO_PIN_0;
        gpio_init_struct.Mode = GPIO_MODE_AF_PP;            /* 复用推挽输出 */
        gpio_init_struct.Pull = GPIO_PULLDOWN;              /* 下拉 */
        gpio_init_struct.Speed = GPIO_SPEED_FREQ_HIGH;      /* 高速 */
        HAL_GPIO_Init(GPIOA, &gpio_init_struct);

        HAL_NVIC_SetPriority(TIM5_IRQn, 1, 3);              /* 抢占1，子优先级3 */
        HAL_NVIC_EnableIRQ(TIM5_IRQn);                      /* 开启ITMx中断 */
    }
}

/* 输入捕获状态(g_timxchy_cap_sta)
 * [7]  :0,没有成功的捕获;1,成功捕获到一次.
 * [6]  :0,还没捕获到高电平;1,已经捕获到高电平了.
 * [5:0]:捕获高电平后溢出的次数,最多溢出63次,所以最长捕获值 = 63*65536 + 65535 = 4194303
 *       注意:为了通用,我们默认ARR和CCRy都是16位寄存器,对于32位的定时器(如:TIM5),也只按16位使用
 *       按1us的计数频率,最长溢出时间为:4194303 us, 约4.19秒
 *
 *      (说明一下：正常32位定时器来说,1us计数器加1,溢出时间:4294秒)
 */
uint8_t g_timxchy_cap_sta = 0;    /* 输入捕获状态 */
uint16_t g_timxchy_cap_val = 0;   /* 输入捕获值 */


/* 定时器5中断服务函数 */
void TIM5_IRQHandler(void)
{
    HAL_TIM_IRQHandler(&g_timx_cap_chy_handle);  /* 定时器HAL库共用处理函数 */
}

/* 定时器输入捕获中断处理回调函数 */
void HAL_TIM_IC_CaptureCallback(TIM_HandleTypeDef *htim)
{
    if (htim->Instance == TIM5)
    {
        if ((g_timxchy_cap_sta & 0X80) == 0)                /* 还没有成功捕获 */
        {
            if (g_timxchy_cap_sta & 0X40)                   /* 捕获到一个下降沿 */
            {
                g_timxchy_cap_sta |= 0X80;                  /* 标记成功捕获到一次高电平脉宽 */
                g_timxchy_cap_val = HAL_TIM_ReadCapturedValue(&g_timx_cap_chy_handle, TIM_CHANNEL_1);  /* 获取当前的捕获值 */
                TIM_RESET_CAPTUREPOLARITY(&g_timx_cap_chy_handle, TIM_CHANNEL_1);                      /* 一定要先清除原来的设置 */
                TIM_SET_CAPTUREPOLARITY(&g_timx_cap_chy_handle, TIM_CHANNEL_1, TIM_ICPOLARITY_RISING); /* 配置TIM5通道1上升沿捕获 */
            }
            else /* 还未开始,第一次捕获上升沿 */
            {
                g_timxchy_cap_sta = 0;                              /* 清空 */
                g_timxchy_cap_val = 0;
                g_timxchy_cap_sta |= 0X40;                          /* 标记捕获到了上升沿 */
                __HAL_TIM_DISABLE(&g_timx_cap_chy_handle);          /* 关闭定时器5 */
                __HAL_TIM_SET_COUNTER(&g_timx_cap_chy_handle, 0);   /* 定时器5计数器清零 */
                TIM_RESET_CAPTUREPOLARITY(&g_timx_cap_chy_handle, TIM_CHANNEL_1);   /* 一定要先清除原来的设置！！ */
                TIM_SET_CAPTUREPOLARITY(&g_timx_cap_chy_handle, TIM_CHANNEL_1, TIM_ICPOLARITY_FALLING); /* 定时器5通道1设置为下降沿捕获 */
                __HAL_TIM_ENABLE(&g_timx_cap_chy_handle);           /* 使能定时器5 */
            }
        }
    }
}

/* 定时器更新中断回调函数 */
void HAL_TIM_PeriodElapsedCallback(TIM_HandleTypeDef *htim)
{
    if (htim->Instance == TIM5)
    {
        if ((g_timxchy_cap_sta & 0X80) == 0)            /* 还未成功捕获 */
        {
            if (g_timxchy_cap_sta & 0X40)               /* 已经捕获到高电平了 */
            {
                if ((g_timxchy_cap_sta & 0X3F) == 0X3F) /* 高电平太长了 */
                {
                    TIM_RESET_CAPTUREPOLARITY(&g_timx_cap_chy_handle, TIM_CHANNEL_1);                     /* 一定要先清除原来的设置 */
                    TIM_SET_CAPTUREPOLARITY(&g_timx_cap_chy_handle, TIM_CHANNEL_1, TIM_ICPOLARITY_RISING);/* 配置TIM5通道1上升沿捕获 */
                    g_timxchy_cap_sta |= 0X80;          /* 标记成功捕获了一次 */
                    g_timxchy_cap_val = 0XFFFF;
                }
                else      /* 累计定时器溢出次数 */
                {
                    g_timxchy_cap_sta++;
                }
            }
        }
    }
}


#include "./SYSTEM/sys/sys.h"
#include "./SYSTEM/usart/usart.h"
#include "./SYSTEM/delay/delay.h"
#include "./BSP/LED/led.h"
#include "./BSP/TIME/gtim.h"

extern g_timxchy_cap_sta = 0;    /* 输入捕获状态 */
extern g_timxchy_cap_val = 0;   /* 输入捕获值 */


int main(void)
{
    uint32_t temp = 0;
    uint8_t t = 0;

    HAL_Init();
    sys_stm32_clock_init(RCC_PLL_MUL9);
    delay_init(72);
    usart_init(115200);
    led_init();
    //ARR = 0xFFFF = 65535
    gtim_timx_cap_chy_init(0xFFFF,72-1);

    while(1)
    {
        if(g_timxchy_cap_sta & 0x80)
        {
            temp = g_timxchy_cap_sta & 0x3F;
            temp *= 65536;
            temp += g_timxchy_cap_val;
            printf("HIGH:%d  us\r\n",temp);
            g_timxchy_cap_sta = 0;
        }

        t++;

        if(t > 20)
        {
            t = 0;
            LED0_TOGGLE();
        }
        delay_ms(10);

    }
}


```

[[程序运行逻辑及其过程变量的变换]]

### 通用定时器脉冲计数实验配置步骤

![[TapTapLoot_IfRkqA4csN.png]]

![[TapTapLoot_goDxmLfTZ2.png]]

```c
typedef struct 
{     
uint32_t SlaveMode;           /* 从模式选择 */     
uint32_t InputTrigger;       /* 输入触发源选择 */     
uint32_t TriggerPolarity;    /* 输入触发极性 */     
uint32_t TriggerPrescaler;   /* 输入触发预分频 */     
uint32_t TriggerFilter;      /* 输入滤波器设置 */ 
} TIM_SlaveConfigTypeDef;
```

## 高级定时器

高级定时器:TIM1/TIM8

主要特性:16位递增,递减,中心对齐计数器(2^16 = 65536 = 0~65535) 
                  16位预分频器(分频系数1~65536)
                  可用于触发DAC,ADC
                  在更新事件(计数器溢出),触发事件,输入捕获,输出比较时,可产生中断/DMA请求
                  4个独立通道,可用于:输入捕获,输出比较,输出PWM,单脉冲模式
                  使用外部信号控制定时器且可实现多个定时器互连的同步电路
                  支持编码器和霍尔传感器电路等
重复计数器:计数器每次上溢或下溢,都能使重复计数器减一,减到零时,再发生一次溢出就会产生更新事件
死区时间带可编程的互补输出

四类时钟源: 内部时钟,内部触发输入,外部引脚输入时钟(外部时钟模式二),      通道1和通道2(外部时钟模式一)

计数器每次上溢或下溢,都能使重复计数器减一,减到零时,再发生一次溢出就会产生更新事件

![[TapTapLoot_irLBywBnZN.png]]

重复计数器有影子寄存器,发生软件产生的中断后,寄存器中的值会重新装载在影子寄存器中,所有还要重复产生溢出

### 高级定时器输出指定个数的PWM实验配置步骤

![[TapTapLoot_eWuLArqeHw.png]]

![[TapTapLoot_NeiR5PTYbr.png]]

```c
typedef struct 
{    
uint32_t OCMode; 	         /* 输出比较模式选择 */   
uint32_t Pulse; 	         /* 设置比较值 */   
uint32_t OCPolarity;       /* 设置输出比较极性 */   
uint32_t OCNPolarity;      /* 设置互补输出比较极性 */   
uint32_t OCFastMode;       /* 使能或失能输出比较快速模式 */   
uint32_t OCIdleState;      /* 空闲状态下OC1输出 */   
uint32_t OCNIdleState;     /* 空闲状态下OC1N输出 */ 
} TIM_OC_InitTypeDef;
```

### 高级定时器输出比较实验

![[TapTapLoot_8qkCRepmpU.png]]

![[TapTapLoot_GtZWpSBUV5.png]]

![[TapTapLoot_o5gFqz9AYq.png]]

```c
typedef struct 
{    
uint32_t OCMode; 	         /* 输出比较模式选择 */   
uint32_t Pulse; 	         /* 设置比较值 */   
uint32_t OCPolarity;       /* 设置输出比较极性 */   
uint32_t OCNPolarity;      /* 设置互补输出比较极性 */   
uint32_t OCFastMode;       /* 使能或失能输出比较快速模式 */   
uint32_t OCIdleState;      /* 空闲状态下OC1输出 */   
uint32_t OCNIdleState;     /* 空闲状态下OC1N输出 */ 
} TIM_OC_InitTypeDef;
```

### 互补输出/带死区的互补输出

由于元器件是有延迟特性,所有要加上死区时间控制,防止电机短暂时间的短路现象

![[TapTapLoot_6ZGchx0aOt.png]]

![[TapTapLoot_NMDEKsVAI5.png]]

![[TapTapLoot_vuLvOaBkW5.png]]

```c
typedef struct 
{    
uint32_t OCMode; 	         /* 输出比较模式选择 */   
uint32_t Pulse; 	         /* 设置比较值 */   
uint32_t OCPolarity;       /* 设置输出比较极性 */   
uint32_t OCNPolarity;      /* 设置互补输出比较极性 */   
uint32_t OCFastMode;       /* 使能或失能输出比较快速模式 */   
uint32_t OCIdleState;      /* 空闲状态下OCx输出 */   
uint32_t OCNIdleState;     /* 空闲状态下OCxN输出 */ 
} TIM_OC_InitTypeDef;
```

```c
typedef struct 
{    
uint32_t OffStateRunMode;    /* 运行模式下的关闭状态选择 */     
uint32_t OffStateIDLEMode;   /* 空闲模式下的关闭状态选择 */     
uint32_t LockLevel; 		     /* 寄存器锁定设置 */     
uint32_t DeadTime; 	         /* 死区时间设置 */     
uint32_t BreakState; 	       /* 是否使能刹车功能 */     
uint32_t BreakPolarity;		   /* 刹车输入极性 */     
uint32_t BreakFilter; 		   /* 刹车输入滤波器(F1/F4系列没有) */     
uint32_t AutomaticOutput; 	 /* 自动恢复输出使能，即使能AOE位 */
} TIM_BreakDeadTimeConfigTypeDef;
```

### 高级定时器PWM输入模式实验

![[TapTapLoot_2pLrE2PrVP.png]]

![[TapTapLoot_KP21n4EDTk.png]]

```c
typedef struct
{     
uint32_t ICPolarity;  /* 输入捕获触发方式选择，比如上升、下降沿捕获 */     
uint32_t ICSelection; /* 输入捕获选择，用于设置映射关系 */     
uint32_t ICPrescaler; /* 输入捕获分频系数 */     
uint32_t ICFilter;    /* 输入捕获滤波器设置 */ 
} TIM_IC_InitTypeDef;
```

```c
typedef struct 
{     
uint32_t SlaveMode;          /* 从模式选择 */     
uint32_t InputTrigger;       /* 输入触发源选择 */     
uint32_t TriggerPolarity;    /* 输入触发极性 */     
uint32_t TriggerPrescaler;   /* 输入触发预分频 */     
uint32_t TriggerFilter;      /* 输入滤波器设置 */ 
} TIM_SlaveConfigTypeDef;
```

- `HAL_TIM_IC_Start_IT`：使能通道的捕获，并且使能该通道的中断（CCxIE置位）。
- `HAL_TIM_IC_Start`：只使能捕获，但不使能中断。硬件捕获仍然会更新CCRx寄存器，但不会产生中断。

所以你可以用`Start`正常捕获，用`Start_IT`开启需要中断的那个通道。两者可以独立控制。

| 通道 | 中断 | 原因 |
| --- | --- | --- |
| 通道1（上升沿） | 开启 | 用于标志一个周期结束，同时读取CCR1（周期）和CCR2（高电平）——两个值都属于**同一个已完成周期** |
| 通道2（下降沿） | 不开启 | 硬件自动更新CCR2，不需要中断干扰；如果在通道2中断中读取，会导致周期和脉宽不匹配 |

| 序号 | 错误 | 文件/位置 | 具体问题 | 解决方案 |
| --- | --- | --- | --- | --- |
| **1** | **未启动PWM输出** | `gtim.c` | 只调用了`HAL_TIM_PWM_Init()`，没有配置输出通道（`OCMode`、`Pulse`等），也没有调用`HAL_TIM_PWM_Start()`启动波形发生。导致PB5没有PWM信号。 | 在`HAL_TIM_PWM_Init`后增加：`c<br>TIM_OC_InitTypeDef oc = {0};<br>oc.OCMode = TIM_OCMODE_PWM1;<br>oc.Pulse = arr/2;<br>oc.OCPolarity = TIM_OCPOLARITY_LOW;<br>HAL_TIM_PWM_ConfigChannel(&g_timx_pwm_chy_handle, &oc, TIM_CHANNEL_2);<br>HAL_TIM_PWM_Start(&g_timx_pwm_chy_handle, TIM_CHANNEL_2);<br>` |
| **2** | **IC2配置写错成员** | `atim.c` | 配置下降沿通道时，将`ICSelection`误写为`ICPrescaler`：`tim_ic_pwmin_chy.ICPrescaler = TIM_ICSELECTION_INDIRECTTI;`导致IC2无法正确映射到TI1，捕获中断不触发。 | 改为：`tim_ic_pwmin_chy.ICSelection = TIM_ICSELECTION_INDIRECTTI;` |
| **3** | **TIM3重映射宏错误** | `gtim.c` / `HAL_TIM_PWM_MspInit` | 使用了`__HAL_AFIO_REMAP_TIM3_ENABLE()`（完全重映射），将TIM3_CH2映射到**PC7**，而非教程所期望的**PB5**。导致实测PB5没有波形，硬件连接PB5→PC6失效。 | 改为：`__HAL_AFIO_REMAP_TIM3_PARTIAL()`（部分重映射，CH2→PB5）或手动操作寄存器：`AFIO->MAPR |
| **4** | **输入引脚GPIO上下拉配置不当** | `atim.c` / `HAL_TIM_IC_MspInit` | 将PC6设为`GPIO_PULLUP`（上拉），教程中用的是`GPIO_PULLDOWN`。虽然没有根本性影响，但为防止浮空干扰，建议按教程使用下拉。 | 将`gpio_init_struct.Pull = GPIO_PULLUP`改为`GPIO_PULLDOWN` |
| **5** | **捕获回调未加+1+1补偿** | `atim.c` / `HAL_TIM_IC_CaptureCallback` | 读取捕获值时，教程加了`+1+1`以补偿硬件延时，你未加。虽然不影响捕获是否触发，但会导致计算的时间略偏小。 | 改为：`g_timxchy_pwmin_hval = HAL_TIM_ReadCapturedValue(..., TIM_CHANNEL_2) + 1 + 1;g_timxchy_pwmin_cval = HAL_TIM_ReadCapturedValue(..., TIM_CHANNEL_1) + 1 + 1;` |
| **6** | **manual CCR2写入位置** | `main.c` | `TIM3->CCR2 = 3;`直接写寄存器，但如果未调用`HAL_TIM_PWM_ConfigChannel`设置OC模式，该赋值无效（后来修正）。此处在加入ConfigChannel后已无问题，但建议统一用`__HAL_TIM_SET_COMPARE`宏管理。 | 无需修改（已有效），或改用：`__HAL_TIM_SET_COMPARE(&g_timx_pwm_chy_handle, TIM_CHANNEL_2, 3);` |