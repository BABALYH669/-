---
notion-id: 33d5d378-8ef5-805b-8278-d32144e73684
---
### AFIO_EXTICR 寄存器 HAL库会自动配置

映射由 **AFIO 外设** 的 **EXTICR1~EXTICR4** 控制（必须先开 AFIO 时钟）。

### 1. 寄存器分配

- **EXTICR1** → EXTI0 ~ EXTI3
- **EXTICR2** → EXTI4 ~ EXTI7
- **EXTICR3** → EXTI8 ~ EXTI11
- **EXTICR4** → EXTI12 ~ EXTI15

---

**给这根 EXTIx 选择具体端口（PA / PB / PC…）**

AFIO 的作用就是：
**EXTI0 你要接 PA0 还是 PB0 还是 PC0？你来选。**

结构是这样：

- AFIO_EXTICR1 → 管 EXTI0 ~ EXTI3
- AFIO_EXTICR2 → 管 EXTI4 ~ EXTI7
- AFIO_EXTICR3 → 管 EXTI8 ~ EXTI11
- AFIO_EXTICR4 → 管 EXTI12 ~ EXTI15

每 **4 位** 配置 **1 根 EXTI 线**：

例如：

- 配置 **EXTI5**
→ 去 EXTICR2
→ 找到 EXTI5 对应的 4 位
→ 写入 0000=PA、0001=PB、0010=PC……

---

### 用一个例子彻底巩固

你要用 **PC13** 做外部中断：

1. **Pin13 → 固定对应 EXTI13**
2. 去 AFIO 里：
    - 找 EXTICR4（因为 EXTI13 在 12~15 区间）
    - 找到 EXTI13 对应的 4 位
    - 写入 **0010** 表示选择 GPIOC
    - 0000=PA、0001=PB、0010=PC 、0011=PD、0100=PE
这样就完成了：
**EXTI13 ↔ PC13** 的连接。

![[image.png]]

![[BongoCat_V0OrIE2nDR.png]]

![[BongoCat_6AYKD2wtt9.png]]

![[BongoCat_UyZTRHplCK.png]]

相应优先级 0级别最高 3最低

```c
6.设置中断服务函数
		void 中断函数名(void)
		{
			中断函数名和中断号的选择规则一样		
			7.中断处理公共函数 __weak弱定义函数,允许用户重写
			HAL_GPIO_EXTI_IRQHandler(写入GPIO_PIN_引脚数);	
			在退出回调函数前，再强制清空一次标志！
			彻底杜绝抖动带来的重复触发！
			__HAL_GPIO_EXTI_CLEAR_IT(写入GPIO_PIN_引脚数);
}

8.Callback函数                    中断引脚号⬇
		void HAL_GPIO_EXTI_Callback(uint16_t GPIO_Pin)
		{
		触发中断号->中断服务函数->中断处理服务函数->Callback函数
		Callback被调用了就证明中断触发了,在这里编写中断函数
		9.判断哪一个EXTI线进来了
		if(GPIO_Pin == GPIO_PIN_引脚号)
		{
			if(HAL_GPIO_ReadPin(GPIOx,GPIO_PIN_引脚号) == 0)
			{
				10.电平翻转->控制LED灯,蜂鸣器等
				HAL_GPIO_TogglePin(GPIOx,GPIO_PIN_引脚号);
			}
		}			
}

void extix_init(void)
{
    GPIO_InitTypeDef gpio_init_struct; //初始化GPIO结构体
    
    1.使能GPIO时钟
    KEY_GPIO_CLK_ENABLE();//时钟使能,再key.h文件中宏定义了
    => __HAL_RCC_GPIOx_CLK_ENABLE(); x=A~G
    
    2.GPIO初始化
	    给了上拉电阻就用下降沿触发
	    给了下拉建组急用上升沿触发
    gpio_init_struct.Pin = GPIO_PIN_3;//PE3引脚
    gpio_init_struct.Mode = ;//模式:上升沿,下降沿,双边沿
    gpio_init_struct.Pull = ;//上拉电阻 下拉电阻
    
    HAL_GPIO_Init(GPIOE,&gpio_init_struct);
		第一个参数指定要初始化的GPIO端口，这里是PE口（按键所在端口）
		gpio_init_struct是上面写的参数 Pin Mode Pull Speed
		
		3.设置中断分组
		这里应该有一步是设置中断分组,这一步在stm32f1xx_hal.c文件中定义过了
		一个工程项目最多定义一个,一般都是分组2 4个抢断优先级,4个响应优先级
	
		4.设置中断优先级
		HAL_NVIC_SetPriority(中断号,抢断优先级,响应优先级);
		中断号要看使用的是哪个端口引脚
		比如是Px4引脚,EXTI4_IRQn 
		比如是Px10引脚,EXTI15_10_IRQN 其中Px10~15都用这个中断号
		x是由上面HAL_GPIO_Init决定的 HAL_GPIO_Init(GPIOx,&gpio_init_struct);
		
		5.使能中断
		HAL_NVIC_EnableIRQ(中断号);
}
		
```

```c
#include "./SYSTEM/sys/sys.h"
#include "./SYSTEM/delay/delay.h"
#include "./BSP/LED/led.h"
#include "./BSP/BEEP/beep.h"
#include "./BSP/KEY/key.h"
#include "./BSP/EXTI/exti.h"
/************************************************************/
//设置中断服务函数
void KEY1_INT_IRQHandler(void)
{
    //中断处理公共函数
    HAL_GPIO_EXTI_IRQHandler(KEY1_INT_GPIO_PIN);
    //推出回调函数前再强制清空一次标志
    //彻底杜绝抖动带来的重复触发
    __HAL_GPIO_EXTI_CLEAR_IT(KEY1_INT_GPIO_PIN);
}

void KEY0_INT_IRQHandler(void)
{
    HAL_GPIO_EXTI_IRQHandler(KEY0_INT_GPIO_PIN);
    __HAL_GPIO_EXTI_CLEAR_IT(KEY0_INT_GPIO_PIN);
}

void KEY_UP_INT_IRQHandler(void)
{
    HAL_GPIO_EXTI_IRQHandler(KEY_UP_INT_GPIO_PIN);
    __HAL_GPIO_EXTI_CLEAR_IT(KEY_UP_INT_GPIO_PIN);
}

/************************************************************/
//Callback函数,用于写自己的逻辑的函数
void HAL_GPIO_EXTI_Callback(uint16_t GPIO_Pin)
{
    delay_ms(20);      /* 消抖 */
    switch(GPIO_Pin)
    {
        case KEY0_INT_GPIO_PIN:
            if (KEY0 == 0)
            {
                LED0_TOGGLE();  /* LED0 状态取反 */ 
                LED1_TOGGLE();  /* LED1 状态取反 */ 
            }
            break;
        case KEY1_INT_GPIO_PIN:
            if (KEY1 == 0)
            {
                LED0_TOGGLE();  /* LED0 状态取反 */ 
            }
            break;
        case KEY_UP_INT_GPIO_PIN:
            if (KEY_UP == 1)
            {
                BEEP_TOGGLE();  /* 蜂鸣器状态取反 */ 
            }
            break;
    }
}
/************************************************************/
void extix_init(void)
{
    //初始化GPIO结构体
    GPIO_InitTypeDef gpio_init_struct;
    //使能GPIO时钟
    KEY1_INT_GPIO_CLK_ENABLE();
    KEY0_INT_GPIO_CLK_ENABLE();
    KEY_UP_INT_GPIO_CLK_ENABLE();
    //给了上拉电阻就使用下降沿触发
    gpio_init_struct.Pin = KEY1_INT_GPIO_PIN;
    gpio_init_struct.Mode = GPIO_MODE_IT_FALLING;
    gpio_init_struct.Pull = GPIO_PULLUP;
    HAL_GPIO_Init(KEY1_INT_GPIO_PORT,&gpio_init_struct);
    
    gpio_init_struct.Pin = KEY0_INT_GPIO_PIN;
    gpio_init_struct.Mode = GPIO_MODE_IT_FALLING;
    gpio_init_struct.Pull = GPIO_PULLUP;
    HAL_GPIO_Init(KEY0_INT_GPIO_PORT,&gpio_init_struct);
    //给了下拉电阻就使用上升沿触发
    gpio_init_struct.Pin = KEY_UP_INT_GPIO_PIN;
    gpio_init_struct.Mode = GPIO_MODE_IT_RISING;
    gpio_init_struct.Pull = GPIO_PULLDOWN;
    HAL_GPIO_Init(KEY_UP_INT_GPIO_PORT,&gpio_init_struct);
    //设置中断分组
    //已经自动设置无需再次设置,默认是[2:2]分组
    
    //设置中断优先级 抢断优先级数字越小优先级越高,响应优先级同理
    HAL_NVIC_SetPriority(KEY0_INT_IRQn,0,2);
    HAL_NVIC_SetPriority(KEY1_INT_IRQn,1,2);
    HAL_NVIC_SetPriority(KEY_UP_INT_IRQn,2,2);
    //使能中断
    HAL_NVIC_EnableIRQ(KEY0_INT_IRQn);
    HAL_NVIC_EnableIRQ(KEY1_INT_IRQn);
    HAL_NVIC_EnableIRQ(KEY_UP_INT_IRQn);
}

```

```javascript
#ifndef __EXTI_H
#define __EXTI_H

#include "./SYSTEM/sys/sys.h"
//INT是interrupt(中断)的缩写,标注此GPIO端口是用于终端功能的KEY0引脚
#define KEY0_INT_GPIO_PORT  GPIOE
#define KEY0_INT_GPIO_PIN   GPIO_PIN_4
#define KEY0_INT_GPIO_CLK_ENABLE()  do{__HAL_RCC_GPIOE_CLK_ENABLE();}while(0)
#define KEY0_INT_IRQn   EXTI4_IRQn //中断编号
#define KEY0_INT_IRQHandler     EXTI4_IRQHandler //中断服务函数

#define KEY1_INT_GPIO_PORT  GPIOE
#define KEY1_INT_GPIO_PIN   GPIO_PIN_3
#define KEY1_INT_GPIO_CLK_ENABLE()  do{__HAL_RCC_GPIOE_CLK_ENABLE();}while(0)
#define KEY1_INT_IRQn   EXTI3_IRQn      //中断编号
#define KEY1_INT_IRQHandler     EXTI3_IRQHandler //中断服务函数

#define KEY_UP_INT_GPIO_PORT  GPIOA
#define KEY_UP_INT_GPIO_PIN   GPIO_PIN_0
#define KEY_UP_INT_GPIO_CLK_ENABLE()  do{__HAL_RCC_GPIOA_CLK_ENABLE();}while(0)
#define KEY_UP_INT_IRQn   EXTI0_IRQn
#define KEY_UP_INT_IRQHandler     EXTI0_IRQHandler

void extix_init(void);
#endif


```

| 中断号（IRQn） | 对应哪些 EXTI 线 | 对应引脚 |
| --- | --- | --- |
| **EXTI0_IRQn** | EXTI0 | 所有 Px0 |
| **EXTI1_IRQn** | EXTI1 | 所有 Px1 |
| **EXTI2_IRQn** | EXTI2 | 所有 Px2 |
| **EXTI3_IRQn** | EXTI3 | 所有 Px3 |
| **EXTI4_IRQn** | EXTI4 | 所有 Px4 |
| **EXTI9_5_IRQn** | EXTI5 ~ EXTI9 | Px5 ~ Px9 |
| **EXTI15_10_IRQn** | EXTI10 ~ EXTI15 | Px10 ~ Px15 |

| 引脚号 | EXTI 线 | 中断函数名 |
| --- | --- | --- |
| 0 | EXTI0 | EXTI0_IRQHandler |
| 1 | EXTI1 | EXTI1_IRQHandler |
| 2 | EXTI2 | EXTI2_IRQHandler |
| 3 | EXTI3 | EXTI3_IRQHandler |
| 4 | EXTI4 | EXTI4_IRQHandler |
| 5~9 | EXTI5~9 | EXTI9_5_IRQHandler |
| **10~15** | **EXTI10~15** | **EXTI15_10_IRQHandler** |
## 中断总结

### 是什么
中断让 CPU **不用轮询死等**，外设有事会主动通知，CPU 暂停手头工作去处理，处理完自动回来。
### 核心三句话
1. **触发由硬件决定**（不是你调用，是引脚电平变化自动触发）
2. **CPU 自动响应**（不需要你在主循环里写任何检测代码）
3. **处理完自动返回**（主程序不知道被打断过）
### 编程 5 步走

| 步骤 | 干什么 | 关键词 |
|------|--------|--------|
| ① 初始化 GPIO | 设引脚为中断模式、触发边沿、上下拉 | `GPIO_MODE_IT_FALLING` |
| ② 设优先级+使能 | 多个中断谁先处理，总开关打开 | `NVIC_SetPriority` + `EnableIRQ` |
| ③ 中断服务函数 ISR | 清标志，调 HAL 公共处理 | 函数名查向量表，不能写错 |
| ④ Callback | **写你的业务逻辑**（翻转 LED 等） | `HAL_GPIO_EXTI_Callback` |
| ⑤ main 初始化一次 | 调用一次 `extix_init()`，然后忘了它 | 主循环不需要轮询 |

### 调用链
```
按键按下 → ISR(清标志) → Callback(你的逻辑) → 返回主程序
```
